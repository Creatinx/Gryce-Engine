<UserControl x:Class="GryceEngine.Editor.Views.ConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:GryceEngine.Editor.Markup"
             xmlns:models="clr-namespace:GryceEngine.Editor.Models"
             xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ══ Panel Header ══ -->
        <Border Grid.Row="0" Style="{StaticResource PanelHeaderStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{loc:Loc panel.console}"
                           Style="{StaticResource PanelHeaderTextStyle}"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="BtnCopy" Width="24" Height="24"
                            ToolTip="Copy selected (Ctrl+C)" Padding="0"
                            Style="{StaticResource ToolbarButtonStyle}"
                            Click="OnCopyClick">
                        <TextBlock Text="&#xE8C8;" FontFamily="Segoe MDL2 Assets" FontSize="10"/>
                    </Button>
                    <Button x:Name="BtnClear" Width="24" Height="24"
                            ToolTip="Clear (Ctrl+L)" Padding="0" Margin="2,0,0,0"
                            Style="{StaticResource ToolbarButtonStyle}"
                            Click="OnClearClick">
                        <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="10"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ══ Log Level Filter Bar ══ -->
        <Border Grid.Row="1"
                Background="{DynamicResource LayerFillColorDefaultBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,0,0,1"
                Padding="6,3">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <ToggleButton x:Name="FilterInfo" IsChecked="True"
                                  Style="{StaticResource ConsoleFilterToggleStyle}"
                                  ToolTip="Show Info" Click="OnFilterChanged">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="7" Height="7" CornerRadius="4"
                                    Background="{DynamicResource TextFillColorSecondaryBrush}"
                                    Margin="0,0,4,0"/>
                            <TextBlock Text="Info" x:Name="InfoCountLabel"/>
                        </StackPanel>
                    </ToggleButton>
                    <ToggleButton x:Name="FilterWarning" IsChecked="True"
                                  Style="{StaticResource ConsoleFilterToggleStyle}"
                                  ToolTip="Show Warnings" Click="OnFilterChanged">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="7" Height="7" CornerRadius="4"
                                    Background="{DynamicResource SystemFillColorCautionBrush}"
                                    Margin="0,0,4,0"/>
                            <TextBlock Text="Warning" x:Name="WarningCountLabel"/>
                        </StackPanel>
                    </ToggleButton>
                    <ToggleButton x:Name="FilterError" IsChecked="True"
                                  Style="{StaticResource ConsoleFilterToggleStyle}"
                                  ToolTip="Show Errors" Click="OnFilterChanged">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="7" Height="7" CornerRadius="4"
                                    Background="{DynamicResource SystemFillColorCriticalBrush}"
                                    Margin="0,0,4,0"/>
                            <TextBlock Text="Error" x:Name="ErrorCountLabel"/>
                        </StackPanel>
                    </ToggleButton>
                </StackPanel>
                <TextBlock Grid.Column="2" x:Name="EntryCount"
                           Text="0 entries"
                           FontSize="10"
                           Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- ══ Log Output List ══ -->
        <ListBox Grid.Row="2"
                 x:Name="LogListBox"
                 BorderThickness="0"
                 Background="Transparent"
                 Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 ItemsSource="{Binding LogEntries}"
                 VirtualizingPanel.ScrollUnit="Pixel"
                 VirtualizingPanel.IsVirtualizing="True"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 SelectionChanged="OnLogSelectionChanged">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                        BorderThickness="0"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background"
                                                Value="{DynamicResource SubtleFillColorSecondaryBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background"
                                                Value="{DynamicResource AccentFillColorSecondaryBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type models:LogEntry}">
                    <Grid Margin="0,1" VerticalAlignment="Top">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <!-- Timestamp + Level label -->
                        <StackPanel Grid.Column="0" Orientation="Horizontal"
                                    VerticalAlignment="Top" Margin="8,3,4,0">
                            <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:HH:mm:ss}'}"
                                       FontFamily="Consolas" FontSize="10.5"
                                       Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                       Width="60"/>
                            <TextBlock Text="{Binding LevelText}"
                                       FontFamily="Consolas" FontSize="9"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center" Margin="4,0,0,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground"
                                                Value="{DynamicResource TextFillColorTertiaryBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Level}" Value="Warning">
                                                <Setter Property="Foreground"
                                                        Value="{DynamicResource SystemFillColorCautionBrush}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Level}" Value="Error">
                                                <Setter Property="Foreground"
                                                        Value="{DynamicResource SystemFillColorCriticalBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                        <!-- Level dot -->
                        <Border Grid.Column="1" Width="5" Height="5" CornerRadius="3"
                                VerticalAlignment="Top" Margin="0,7,6,0">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Level}" Value="Warning">
                                            <Setter Property="Background"
                                                    Value="{DynamicResource SystemFillColorCautionBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Level}" Value="Error">
                                            <Setter Property="Background"
                                                    Value="{DynamicResource SystemFillColorCriticalBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Level}" Value="Info">
                                            <Setter Property="Background"
                                                    Value="{DynamicResource TextFillColorSecondaryBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                        <!-- Message -->
                        <TextBlock Grid.Column="2"
                                   Text="{Binding Message}"
                                   FontFamily="Consolas" FontSize="10.5"
                                   TextWrapping="Wrap"
                                   VerticalAlignment="Top"
                                   Margin="0,2,8,2">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground"
                                            Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Level}" Value="Warning">
                                            <Setter Property="Foreground"
                                                    Value="{DynamicResource SystemFillColorCautionBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Level}" Value="Error">
                                            <Setter Property="Foreground"
                                                    Value="{DynamicResource SystemFillColorCriticalBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- ══ Detail Panel (collapsed by default) ══ -->
        <Border Grid.Row="3"
                x:Name="DetailPanel"
                Background="{DynamicResource LayerFillColorDefaultBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0"
                Visibility="Collapsed"
                Padding="10,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" x:Name="DetailText"
                           FontFamily="Consolas" FontSize="10.5"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                           VerticalAlignment="Center"/>
                <Button Grid.Column="1" x:Name="BtnCopyDetail"
                        Width="24" Height="24" Padding="0"
                        Style="{StaticResource ToolbarButtonStyle}"
                        ToolTip="Copy full message"
                        Click="OnCopyDetailClick">
                    <TextBlock Text="&#xE8C8;" FontFamily="Segoe MDL2 Assets" FontSize="10"/>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
