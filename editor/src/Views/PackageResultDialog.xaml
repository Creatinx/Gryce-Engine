<Window x:Class="GryceEngine.Editor.Views.PackageResultDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:GryceEngine.Editor.Markup"
        xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
        ui:ThemeManager.IsThemeAware="True"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="{loc:Loc package.success_title}"
        Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False"
        Background="{DynamicResource SolidBackgroundFillColorBaseBrush}">
    <StackPanel Margin="20,16">
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock x:Name="IconText" Text="&#xE73E;" FontFamily="Segoe MDL2 Assets"
                       FontSize="26" Foreground="{DynamicResource AccentFillColorDefaultBrush}"
                       VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock x:Name="MessageText" TextWrapping="Wrap" FontSize="13"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <Border Background="{DynamicResource LayerFillColorAltBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1" CornerRadius="4" Padding="10,7">
            <TextBlock x:Name="PathText" FontFamily="Consolas" FontSize="11.5"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       TextTrimming="CharacterEllipsis"/>
        </Border>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button x:Name="OpenFolderButton" Content="{loc:Loc package.open_folder}"
                    MinWidth="92" MinHeight="30" FontSize="12" Margin="0,0,8,0"
                    Click="OnOpenFolderClick"/>
            <Button x:Name="CancelButton" Content="{loc:Loc common.cancel}"
                    MinWidth="88" MinHeight="30" FontSize="12" Margin="0,0,8,0"
                    IsCancel="True" Click="OnCancelClick"/>
            <Button x:Name="OkButton" Content="{loc:Loc common.ok}"
                    MinWidth="88" MinHeight="30" FontSize="12"
                    Style="{StaticResource AccentButtonStyle}"
                    IsDefault="True" Click="OnOkClick"/>
        </StackPanel>
    </StackPanel>
</Window>
