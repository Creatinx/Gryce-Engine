# 文件名：physics_world_3d.h

## 文件作用

这个文件声明了 3D 物理世界的通用接口（叫 IPhysicsWorld3D）。它定下了"一个 3D 物理世界应该能做什么"的清单，比如初始化、每帧往前推进、改变重力、创建和删除刚体、给刚体改位置和速度、加力加推力、创建碰撞体、做射线检测、创建关节，还有把碰撞和触发器事件取回来。具体怎么做它不管，由底下不同的物理引擎去实现。文件末尾还声明了一个制造 3D 物理世界的函数（create_physics_world_3d），按名字返回一个具体物理引擎做出来的世界对象。

---