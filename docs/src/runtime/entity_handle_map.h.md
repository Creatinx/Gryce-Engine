# 文件名：entity_handle_map.h

## 文件作用

这个文件是干什么的：它声明了一个叫 EntityHandleMap（实体编号对应表）的类型，负责把一个对外公开的简单数字编号跟场景里实体真正的 UUID 编号对应起来，方便外部不用知道太多内部细节就能指到某个实体。它列出了这张表可以做什么：给一个 UUID 分配一个新数字编号、把某个数字编号对应的关系删掉、把整张表清空重新来、通过数字编号查出对应的 UUID、也能反过来用 UUID 查到数字编号，还能一次性把整张表从场景重建出来，以及列出所有还有效的数字编号。因为可能被多人同时用，这张表带了一把锁来保证安全。

---