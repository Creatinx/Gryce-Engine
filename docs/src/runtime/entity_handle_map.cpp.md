# 文件名：entity_handle_map.cpp

## 文件作用

这个文件是干什么的：它是 entity_handle_map.h 里声明的那些方法的实际实现。它负责真正给一个 UUID 分配一个新数字编号并记下对应关系，删除某个数字编号时把两边的对应都清理掉，以及把整张表清空重新计数。它还实现了通过数字编号查出 UUID、用 UUID 反过来查出数字编号、一次性从整个场景把所有实体登记进表里、列出现有全部数字编号的实现。因为可能被多人同时使用，每次读写都上了锁来保证不会乱套。

---