# 文件名：command_buffer.h

## 文件作用

这个文件是干什么的：它声明了一个叫 CommandBuffer（命令队列）的类型，用来在两条线（编辑线程和引擎主线程）之间传递一连串的命令，比如暂停、选中实体这些操作。它用的是两块缓冲区轮流换的办法，一个往里面加命令，另一个拿出旧命令处理，中间不用一直等对方。它声明了这些接口：往队列里塞一条命令、一次塞一批命令、两块缓冲区换一下身份、把换出来的一批命令拿出来处理、看还有多少地方能塞命令、以及看看上一轮丢掉了多少命令。

---