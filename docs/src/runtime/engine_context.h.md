# 文件名：engine_context.h

## 文件作用

这个文件是干什么的：它定义了引擎运行时用的一组全局信息和设置，也就是引擎在运行时一共记了哪些东西。它声明了一个叫 EngineContext 的大结构，里面装着正在运行的世界、用来往下发命令的队列、实体编号对应表、回调表、当前的运行/暂停状态、选中的实体、正在用的场景等。它还定义了怎么把键盘鼠标的输入记下来（按键、鼠标位置、鼠标锁定等），以及输入事件用什么样的格式，并声明了一些帮外部把实体编号解析成内部实体、把组件名字整理成方便查找形式的小工具。

---