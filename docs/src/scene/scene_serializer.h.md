# 文件名：scene_serializer.h

## 文件作用

这个文件是干什么的：它声明了 SceneSerializer（场景保存读写工具）类型，负责把整个场景变成可以存下来的数据（JSON），也能反过来从这些数据重新建出场景。它声明了几个主要接口：把场景或者单个实体变成 JSON 数据、从 JSON 数据重新建出场景、把场景保存成文件、从文件读回场景。底层靠 JSON 格式来做这件事。

---