# 文件名：prefab.cpp

## 文件作用

这个文件是干什么的：它是 prefab.h 里声明的那些方法的实际实现。它负责把预制体模板从文件读进来并记住在内存里方便反复用，把实体子树保存成预制体文件，把模板照搬成场景里的一套实体并挂上记录来源的标记。它还实现了给照搬出来的实例改动名字、开关、位置、组件等各项参数，在读取过程中防止模板自己套自己（循环引用），以及把一个实例还原回模板加改动的原始状态。

---