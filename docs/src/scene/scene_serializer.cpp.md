# 文件名：scene_serializer.cpp

## 文件作用

这个文件是干什么的：它是 scene_serializer.h 里声明的那些方法的实际实现。它负责把场景里每个实体（名字、编号、开关状态、位置、各种组件）都转成可以存下来的 JSON 数据，再把这些数据写进文件里；反过来读文件时，它把 JSON 数据拆开，一个个重建出实体，并按文件里写的父级关系把它们重新拼成树，还要防止有人故意做出诡异的父子关系导致出错。它还处理预制体照搬进场景时的保存和还原。

---