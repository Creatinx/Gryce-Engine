# 文件名：entity.h

## 文件作用

这个文件是干什么的：它声明了 Entity（实体）类型，代表场景里的一个东西，每个实体自带一个位置信息（Transform），还能挂上很多种其他组件，并且能跟别的实体构成父子关系。它列出了实体可以做什么：起名字、拿编号、加或删组件、加或删子物体、把子物体从层级里摘下来、批量转移子物体、查找某个组件、计算最终在世界里的位置、按顺序走一遍自己和所有子孙、让组件走生命周期步骤（初始化、开始、更新、渲染、销毁），还支持把自己完整复制一份，以及保存和恢复运行时的快照。

---