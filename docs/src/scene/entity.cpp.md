# 文件名：entity.cpp

## 文件作用

这个文件是干什么的：它是 entity.h 里声明的那些方法的实际实现。它负责在新建实体的时候自动加上默认的位置信息和层级信息，管理组件从挂上到卸下的整个流程，处理把实体放到别的实体下面当孩子的各种情况，把实体的位置想办法算成它在世界里的最终位置。它还实现了把整个实体连同子树完整复制一份、把实体的状态存成快照、再把快照恢复回来的方法，以及让生命周期各步骤（初始化、开始、更新、渲染、销毁）一层层传给所有子孙的实现。

---