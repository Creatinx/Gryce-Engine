# 文件名：scene.cpp

## 文件作用

这个文件是干什么的：它是 scene.h 里声明的那些方法的实际实现。它负责真正把实体加进场景、把实体连子树一起删除、把另一个场景当预制体照搬进来，并驱动整个场景的生命周期步骤（初始化、更新、渲染、销毁）。它还实现了只把改动过的部分保存成文件、重新从文件载入场景并尽量保留运行时状态、临时加载子场景再卸载的实现，以及把场景里所有实体登记到组件仓库的办法。

---