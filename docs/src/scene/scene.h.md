# 文件名：scene.h

## 文件作用

这个文件是干什么的：它声明了 Scene（场景）类型，用来把一整组实体放在一起管理，一个场景里所有实体都挂在一个总的根节点下面。它列出了场景可以做什么：新建或者按名字找实体、把实体加进场景、把一个实体连同它的子树整个删掉、把另一个场景当预制体直接照搬进来。它还能记录场景有没有改动过、只把改动过的部分存成文件（差异保存）、重新从文件载入场景并尽量保持运行状态（热重载），以及按需要临时把别的场景加载进来（子场景流送）再卸载掉，最后统一驱动所有实体的生命周期。

---