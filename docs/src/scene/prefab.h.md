# 文件名：prefab.h

## 文件作用

这个文件是干什么的：它声明了 Prefab（预制体）类型，把一整棵实体子树做成一个可以反复用的模板，存在文件里，需要的时候随时能重新造一套出来。它列出了预制体可以做什么：从文件里读模板、把实体子树存成预制体文件、把模板照搬到场景里用、给照搬出来的实例套上各种改动参数、把这些改动去掉恢复成原样、找到实体上记录它来自哪个模板的标记、按模板整理一套对应关系，还有清空模板缓存的办法。

---