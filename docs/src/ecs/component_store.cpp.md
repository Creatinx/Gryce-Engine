# 文件名：component_store.cpp

## 文件作用

这个文件是干什么的：实现 ComponentStore 的具体行为。它负责把组件放进对应类型的数组里，也能随时把某个实体上的某种组件移除，或者一次性清掉一个实体的全部组件。在做这些的同时，它维护着"实体有哪些组件类型"的记录，保证增删时两边保持一致。

查询时，它既可以根据实体去查它是否拥有某种组件，也可以按组件类型把该类型存的一整组组件返回出来，方便系统逐一只取出来使用。

---