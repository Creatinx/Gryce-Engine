# 文件名：gryce_world.cpp

## 文件作用

这个文件是干什么的：提供一套给外部（编辑器等）使用的 World 接口，它里面包着一个真正的 ECS World，方便外部代码操作。外部通过这里创建/销毁世界、创建和删除实体、拿到场景，以及驱动每帧更新。

它的 update 还顺带负责把场景里的模型和光源数据交给渲染器：把还没上传 GPU 的模型推到渲染线程上传，把可见模型的网格、材质、位置交给绘制，把光源收集起来设置给渲染器，让 3D 画面每帧正确地呈现出来。还内置注册了一批游戏自带的组件类型。

---