# 文件名：render_system_3d.h

## 文件作用

这个文件是干什么的：声明了 RenderSystem3D 类，它是一个负责 3D 网格渲染的系统。它负责把场景里的网格模型数据提交给渲染管线，也可以选择通过一个渲染服务器来统一提交和绘制。它在渲染阶段运行。

这个系统可以通过传入渲染管线和渲染服务器来用，还提供一个方法在运行后改设置渲染服务器。它内部保存一张表，用来记录实体和它对应光源的对应关系，避免每帧重复建立光源。

---