# 文件名：script_system.cpp

## 文件作用

这个文件是干什么的：实现 ScriptSystem 的具体工作。它会启动一个全局的脚本虚拟机，并注册引擎、数学等一批底层功能，让脚本可以调用。每一帧它收集所有带脚本组件的实体，按优先级排好先后，依次把脚本加载起来并调用 on_update 等生命周期方法，还会把输入事件派发给脚本处理。

它负责脚本的加载和卸载、调用脚本里的方法、把脚本导出的属性读出来或写回去，以及错误暂停和热重载。脚本运行出错时，它记录错误并暂停该实体，直到重新加载才继续；关闭或切换场景时会把所有已加载脚本连同其模块空间一起卸载干净。

---