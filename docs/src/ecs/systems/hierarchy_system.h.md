# 文件名：hierarchy_system.h

## 文件作用

这个文件是干什么的：声明了 HierarchySystem 类，它是一个维护实体父子关系的系统，在后期更新阶段运行。它的任务是每帧把表示父、子的组件数据整理对，使它们在组件池里保持一致，并为以后计算实体在世界中的位置做准备。

头文件说明这个系统将来还可以做更多事，比如批量传播"谁动了需要重算"的标记、延迟计算世界坐标矩阵、统一派发层级变化通知，但现在的版本主要做父子数据对齐这一件事。

---