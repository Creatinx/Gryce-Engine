# 文件名：render_system_2d.cpp

## 文件作用

这个文件是干什么的：实现 RenderSystem2D 的具体画法。每一帧它收集所有启用的 2D 绘制组件，按图层分组（和 Godot 的画布分层意思差不多），并给每一层找出对应的 2D 摄像机。它先设置环境光和点光源，再画世界层的天空盒作为背景。

然后按每个图层的先后顺序和绘制排序规则，逐个调用组件自己的画法画出来；有 2D 摄像机的层用它的位置视角来画，没有摄像机的非世界层就按屏幕空间来画。画面画完后再把摄像机状态恢复原样。

---