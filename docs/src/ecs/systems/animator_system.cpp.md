# 文件名：animator_system.cpp

## 文件作用

这个文件是干什么的：实现 AnimatorSystem 的具体行为。每一帧它遍历所有带蒙皮模型组件的实体，第一次碰到会先加载模型数据。然后推动动画播放时间（受播放速度和播放开关控制），再根据当前时间和循环设置，算出这一帧每根骨骼的矩阵，把结果放进骨骼矩阵数据里交给渲染使用。

如果某个实体没有动画片段，就给出它的静止姿势。计算出的骨骼矩阵数量超过上限时会被截断。这样角色就能在游戏里动起来。

---