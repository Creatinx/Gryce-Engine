# 文件名：animator_system.h

## 文件作用

这个文件是干什么的：声明了 AnimatorSystem 类，它是一个驱动骨骼动画的系统，在更新阶段处理场景里所有带蒙皮动画组件的实体。它本身只提供一个在每帧更新时被调用的方法，负责把动画播放向前推进并算出当前帧该用的骨骼矩阵。

这个系统用来播放动画片段，并且支持简单地在不同动画片段之间切换。现阶段只做这一件事，动画的混合过渡这些复杂功能还没有实现。

---