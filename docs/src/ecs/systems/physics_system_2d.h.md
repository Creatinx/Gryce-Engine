# 文件名：physics_system_2d.h

## 文件作用

这个文件是干什么的：声明了 PhysicsSystem2D 类，它是一个负责 2D 物理模拟的系统。它把真正算碰撞和运动的事情交给一个底层的 2D 物理世界（目前默认是 Box2D），支持刚体配合各种 2D 碰撞体，还有重力、阻尼和睡眠这些物理行为。

它对外提供设置/读取全局重力、发射射线检测障碍等功能，并规定了固定的物理步长、每个步里的子步数，以及一帧最多推进多少个物理步，用来保证模拟既稳定又不会在卡顿帧失控。

---