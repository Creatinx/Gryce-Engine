# 文件名：physics_system_3d.h

## 文件作用

这个文件是干什么的：声明了 PhysicsSystem3D 类，它是一个负责 3D 物理模拟的系统。它把真正算碰撞和运动的事情交给一个底层的 3D 物理世界（目前默认是 Jolt），支持刚体和静态体配合各种 3D 碰撞体，还有重力、阻尼、睡眠等物理行为。

它对外提供发射射线检测障碍、立即重建某个实体的物理物体、取回本帧发生的碰撞/触发事件等功能，并规定了固定的物理步长、每个步里的子步数以及一帧最多推进的物理步数，保证模拟稳定。

---