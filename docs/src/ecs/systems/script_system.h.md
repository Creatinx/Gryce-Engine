# 文件名：script_system.h

## 文件作用

这个文件是干什么的：声明了 ScriptSystem 类，它是一个负责跑脚本的系统。它把 .js 脚本文件加载进每个脚本组件自己的独立空间里，并调用脚本里写的开始、每帧更新、销毁等方法。这个系统提供了整体重载全部脚本（热重载）、同步和读写脚本属性等方法。

它还带有一个全局的脚本虚拟机（脚本引擎）用来真正运行脚本代码，内部保存着脚本源文件的缓存，以及每个脚本组件对应的已加载模块。这样脚本就能在游戏运行过程中被加载、运行、暂停和卸载。

---