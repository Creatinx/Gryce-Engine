# 文件名：world.cpp

## 文件作用

这个文件是干什么的：实现 World 类的全部具体行为。它会挂载或卸载场景，场景更换后会自动重新启动。注册系统时，如果 World 已启动会立即对新系统做启动处理，避免错过开场时刻。

运行部分：update 会把系统按执行阶段分组、按优先级排好先后，再依次让每个启用的系统处理一帧更新；render 则按渲染阶段依次调用系统的渲染逻辑，最后再让场景组件自行补画。shutdown 会把所有系统逐一套关关闭并销毁场景，防止残留内容带到下一个场景。

---