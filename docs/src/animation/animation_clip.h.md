# 文件名：animation_clip.h

## 文件作用

这个文件是动画片段（AnimationClip）的说明书（头文件）。动画片段就是记录一个角色在动画里每帧每根骨骼的"位置、旋转、缩放"数据的盒子。它声明了单个关键帧 Keyframe、一根骨骼足迹的 BoneTrack，以及真正的动画片段类 AnimationClip。AnimationClip 里声明了怎么按骨骼查轨道、怎么算某一帧的时间，以及怎么从关键帧采样出位置、旋转和最终矩阵的接口。有了它，角色才能做走路、跳舞这类动作。

---