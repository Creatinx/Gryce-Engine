# 文件名：big_number.h

## 文件作用

这个文件是干什么的：它声明了两个类，BigInt 和 BigDecimal，用途是精确处理普通数字算不了的大数。BigInt 用来存任意位数的整数，能从字符串读入、转成字符串，还能加减乘除、取余、乘方、取绝对值、比较大小，另外还有一个专门给 BigDecimal 用的算 10 的 n 次方的小工具。BigDecimal 用来存很多位小数的高精度数字，用 BigInt 存系数再加一个小数位记录，它支持四则运算、乘方、开方、floor/ceil/round 以及设置保留小数位等功能。这两个类主要服务于脚本里的 big 模块。

---