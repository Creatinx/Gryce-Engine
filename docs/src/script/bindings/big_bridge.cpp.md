# 文件名：big_bridge.cpp

## 文件作用

这个文件是干什么的：它把 C++ 里做高精度运算的 big_number 功能接到脚本上，让 JS 里能直接用一个叫 big 的对象。它给脚本准备了很多函数，分成 big.int（处理大整数）和 big.decimal（处理多位小数）两组，包括把字符串转成正数、加减乘除、取余、乘方、取绝对值、比较大小，还有小数开方、四舍五入、floor/ceil 等。整份文件最后把这些函数一个一个挂到 JS 全局的 big 对象上，方便脚本调用。

---