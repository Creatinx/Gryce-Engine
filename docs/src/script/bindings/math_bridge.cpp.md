# 文件名：math_bridge.cpp

## 文件作用

这个文件是干什么的：它给脚本准备了一大堆常用的数学函数，都挂在 JS 的 math 对象下。里面分成几组：基础计算（插值、反插值、重映射、夹住范围、平滑阶跃、取正负号、循环回绕、往返摆动、向目标靠近、平滑阻尼）、角度计算（角度差、角度插值、四舍五入、对齐到步长、近似相等判断），以及一批缓动函数（linear、quad、cubic、sine、expo、back、elastic、bounce 这些的进出和进出结合版本），用来做动画过渡。整份文件把这些函数一个个挂到全局 math 对象上供脚本使用。

---