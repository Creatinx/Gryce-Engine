# 文件名：engine_bridge.cpp

## 文件作用

这个文件是干什么的：它把引擎核心的各种能力接到脚本上，让 JS 里能用 engine 这个对象操作游戏世界。它提供了一大批功能：打日志、查版本号、获取当前脚本所在的实体；管理实体（查名字、按名字找、创建、销毁、换网格、读写位置旋转和大小）；读写组件（有没有、拿出属性、设属性）；存取跨场景的小状态；查输入（按键、鼠标位置和按钮）；看时间；加载场景、查当前场景；还有设置物理重力。其中界面和音效等一部分功能给脚本后还没真正实现，只是留了个空。最后它把这些函数组织成一个 engine 对象挂到 JS 全局里。

---