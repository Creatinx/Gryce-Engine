# 文件名：script_context.h

## 文件作用

这个文件是干什么的：它声明了 ScriptContext 这个类，让脚本运行时能有一份公共信息给脚本调用。它里面存了当前实体、当前场景、界面管理者这些"正在处理谁"的信息，还有一套按键/鼠标状态和上一帧时间、总运行时间的记录。它提供的是直接读写这些信息的方法，例如设置和读取当前实体、跨场景的字符串小数据、按键和鼠标状态、每帧的间隔时间等，还提供一个每帧开始时清空数据的入口。整个工程里只维护一份这样的公共信息。

---