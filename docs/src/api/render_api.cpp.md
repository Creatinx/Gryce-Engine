# 文件名：render_api.cpp

## 文件作用

这个文件提供了一组和"渲染"有关的 C 接口，渲染就是负责把画面画到屏幕上的过程。它负责初始化、关闭整个渲染系统，管理渲染窗口的尺寸，还负责每一帧开始和结束时把当前世界画出来。它收集场景里的主相机和所有光源，控制很多画面效果设置，比如高清范围、色调映射、环境光、阴影、曝光等。它还处理编辑器视图和游戏视图两个画面的显示，以及切换 OpenGL 和 Vulkan 这些底层绘图接口。

---